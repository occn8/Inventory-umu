import 'package:flutter/material.dart';

class InvMgt extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Inventory Management'),),
          body: Container(
         child: Text('inventory management'),
      ),
    );
  }
}